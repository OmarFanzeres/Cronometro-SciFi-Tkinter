# Sci-Fi Stopwatch in Python (Tkinter)

A desktop stopwatch inspired by Star Trek interfaces, built with Python and Tkinter.  
Includes a neon-themed UI, animated energy bar, and Sci-Fi style sound feedback using Windows beeps.

---

## 🚀 Features:

- Dark mode with neon blue-green accent colors
- Animated energy bar using Tkinter Progressbar
- Simple Sci-Fi sound alerts using `winsound`
- Standalone `.exe` generation via PyInstaller (no external dependencies for sounds)

---

## 🛠️ Requirements:

- Python 3.x
- Libraries:
  - `pillow`
  - `requests`
  - (Tkinter comes pre-installed with Python on Windows)

---

## 📦 How to run (from source):

```bash
pip install pillow requests
python cronometro.py
